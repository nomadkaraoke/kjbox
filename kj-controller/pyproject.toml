[project]
name = "kj-controller"
<<<<<<< HEAD
=======
version = "0.115.0"
>>>>>>> 2b0ff4d (Singer self-serve for KJ-added/partner songs + clearer reorder instructions (v0.115.0))
description = "Web-based karaoke show management with mpv + VLC playback"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks integration tests (deselect with '-m \"not integration\"')",
]
addopts = "-ra -q"

[tool.coverage.run]
source = ["."]
omit = [
    "tests/*",
    "venv/*",
]

[tool.coverage.report]
show_missing = true
skip_empty = true
exclude_lines = [
    "if __name__",
    "pragma: no cover",
]
